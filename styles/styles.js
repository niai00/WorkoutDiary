import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
    backgroundColor: '#e8f1bd'
  },
  list: {
    padding: 10,
    margin: 10,
    backgroundColor: '#d7f09c'
  }
});

export default styles;

  
